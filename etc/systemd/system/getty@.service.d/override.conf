[Service]
ExecStartPre=/usr/bin/sh -c 'setleds -D +num < /dev/%I'
ExecStart=
ExecStart=-/usr/bin/agetty --noreset --noclear --nohints --issue-file=/etc/issue.d/issue - ${TERM}
# Have boot messages stay on virtual terminal
TTYVTDisallocate=no
